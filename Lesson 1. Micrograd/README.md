# Lesson 1: Micrograd (Andrey Karpathy)

Этот проект содержит материалы первого урока курса Андрея Карпати по созданию нейросетей с нуля. Мы реализуем `micrograd` — крошечный движок автоматического дифференцирования.

## 🚀 Быстрый старт

Проект использует современный менеджер пакетов [uv](https://github.com/astral-sh/uv) для управления зависимостями и версией Python.

### 1. Установка зависимостей
Убедитесь, что у вас установлен `uv`, затем выполните:
```bash
make install
```
Эта команда:
- Создаст виртуальное окружение `.venv`.
- Установит все необходимые библиотеки (numpy, matplotlib, ipykernel, notebook).
- Зафиксирует версии в `uv.lock`.
- Зарегистрирует Jupyter Kernel под именем **Python 3.13 (Micrograd)**.

### 2. Запуск
Вы можете работать с проектом двумя способами:

#### Через VS Code (Рекомендуется)
1. Откройте файл `lesson.ipynb`.
2. В верхнем правом углу выберите ядро (Select Kernel) -> **Python Environments** -> **.venv (Micrograd)**.

#### Через терминал
```bash
make run
```
Запустится классический интерфейс Jupyter Notebook в браузере.

## 📂 Структура проекта

- `lesson.ipynb` — основной файл с кодом и пояснениями.
- `pyproject.toml` — описание зависимостей проекта.
- `uv.lock` — зафиксированные версии пакетов (гарантирует воспроизводимость).
- `Makefile` — удобные команды для автоматизации (`install`, `run`, `clean`).
- `.python-version` — указывает `uv` использовать Python 3.13.

## 🛠 Полезные команды uv

- Добавить новую библиотеку: `uv add <package_name>`
- Удалить библиотеку: `uv remove <package_name>`
- Обновить все зависимости: `uv sync`
- Форматирование кода (включая ноутбуки): `make format`

## 💡 Ruff в Jupyter Notebooks
Ruff отлично поддерживает работу с `.ipynb` файлами. Команда `make format` автоматически отформатирует ячейки с кодом в вашем ноутбуке, удалит неиспользуемые импорты и исправит мелкие ошибки.
